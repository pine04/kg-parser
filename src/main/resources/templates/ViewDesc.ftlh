@ViewDesc(
        formTitle = "${formTitle}",
        domainClassLabel = "${domainClassLabel}",
        imageIcon = "${imageIcon}",
        view = ${view},
        viewType = ${viewType},
        parent = ${parent}
    )