@DSDesc(
    type = "${type}",
    dsUrl = "${dsUrl}",
    user = "${user}",
    password = "${password}",
    dsmType = ${dsmType},
    domType = ${domType},
    osmType = ${osmType},
    connType = ${connType}
)